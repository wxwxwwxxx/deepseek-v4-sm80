# Pivot Candidate Table

| Lane | Target owner/bucket | Expected gain | Quality risk | Engineering size | Decision |
| --- | --- | ---: | --- | --- | --- |
| Exact-ish HC/router TF32 | HC pre linear `0.178373s`; MoE router `0.097109s`; FP32/SGEMM cluster `0.257269s` | `0.000s` credible at decode-small M=4 | Low, but no speed | Small | Reject. TF32 is process-global in PyTorch and was slower/noisy-negative for M=4. |
| HC/router BF16-like | Same HC/router owners | `0.088714s` theoretical combined, `0.052250s` HC-only | High. Router top-k set overlap fell to `0.992350` on M=1024 and exact order match to `0.854492`. HC output max abs err reached about `0.044` at M=4. | Small-medium | Reject. It is a precision-policy change with visible router movement and no quality path in this target. |
| vLLM-aligned FP8/custom projection-cache cluster | TARGET 07.70 BF16 small-GEMM cluster `0.521012s`: WQA/WKV, shared experts, `wo_a`, `q_wqb`, `wo_b`, indexer projection pieces | Target `0.12s-0.20s` profile reduction; promotion gate should require at least `0.10s` | Medium. Requires explicit per-owner numeric gates plus TP8 text smoke. Does not require full FP8 KV-cache E2E. | Medium-large, but targetable | Select. This maps to a coherent `>=0.35s` cluster and is the best next implementation target. |
| WQA/WKV-only FP8 boundary | attention WQA/WKV/compress `0.119458s` in 07.69, `0.123405s` in 07.70 candidate profile | Below single-owner `0.20s`; useful as a representative inside the selected cluster | Medium | Medium | Do not make standalone next target. Use as one representative/gate inside selected projection-cache cluster. |
| vLLM `fused_inv_rope_fp8_quant` plus `deepseek_v4_fp8_einsum` | Current mini `wo_a` `0.064244s` plus `wo_b` local `0.051837s`; extra `o_rope` elementwise is outside current projection denominator | Below standalone gate on current promoted stack | Medium-high FP8 activation/output-boundary risk | Medium | Hold. 07.62 already collapsed `wo_a`; this no longer maps to a large enough isolated owner. |
| Fused indexer q/rope/quant plus packed FP8 indexer/cache continuation | Current indexer projection pieces around `0.084222s` in projection/GEMM; prior FP8 indexer/select work is already in victory bundle | Below `0.20s` owner and below `0.35s` coherent next cluster after 07.52/07.53 | Medium; top-k/index overlap gate required | Medium | Hold. Continue only if a fresh profile shows indexer/cache has re-grown. |
| Full `fp8_ds_mla` KV-cache E2E | Attention/cache architecture, not a single mini owner in the current 07.69/07.70 projection table | Potentially large, but not isolated | High; cache dtype/layout and attention semantics change | Large | Do not select as next small target. It requires full FP8 KV-cache E2E to validate. |
| vLLM compile/custom-op boundary | `@support_torch_compile`, `torch.ops.vllm.deepseek_v4_attention`, graph-visible wrapper boundaries | No concrete mini owner/cluster mapping from this target | Low-medium if exact, high if coupled to FP8 cache | Large/broad | Reject for now. Previous narrow compile/preflight was too small, and broad compile is out of scope. |

Selected lane: vLLM-aligned FP8/custom projection-cache cluster.
