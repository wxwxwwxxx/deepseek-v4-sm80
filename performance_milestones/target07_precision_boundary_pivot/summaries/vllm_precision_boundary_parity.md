# vLLM Precision / Boundary Parity

This table is a source-boundary comparison against `/workspace/vllm-dsv4-docker`.
It is not a vLLM runtime bucket profile.

| vLLM mechanism | Source file/function | Mini analogue | Mini owner/cluster | Precision contract | Cache/layout requirement | Quality gate | Expected profile surface | Independent target? |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DeepSeek V4 FP8 quantized projection stack | `vllm/model_executor/models/deepseek_v4.py:DeepseekV4FP8Config`; `DeepseekV4Attention.fused_wqa_wkv`; `vllm/model_executor/layers/deepseek_v4_attention.py:DeepseekV4MLAModules.forward` | `python/minisgl/models/deepseek_v4.py:DSV4Attention.forward`; `DSV4Linear`; cached BF16 projection paths for `q_wqb`, `wo_b`, `wo_a`, `indexer.wq_b`, shared experts | BF16 small-GEMM cluster `0.521012s` in TARGET 07.70; baseline `0.521619s` in TARGET 07.69 | vLLM fast path keeps FP8 weights/scales under `deepseek_v4_fp8`; mini currently dequantizes/caches BF16 for exact route | Needs graph-safe FP8/custom projection boundary and scale layout; does not need full FP8 KV-cache E2E for an isolated projection probe | Per-owner output max/mean/p99 error vs promoted BF16, attention/logit smoke, TP8 text smoke, graph replay `>0`, eager decode `0` | `>=0.35s` coherent projection cluster, target `0.12s-0.20s` reduction if the custom boundary beats BF16 small-GEMM overhead | Yes. Selected next target, as a bounded projection-cache/custom-GEMM target. |
| WQA/WKV fused projection boundary | `vllm/model_executor/models/deepseek_v4.py:DeepseekV4Attention.fused_wqa_wkv`; `vllm/model_executor/layers/deepseek_v4_attention.py:forward` line path `qr_kv = self.fused_wqa_wkv(hidden_states)` | Mini fused WQA/WKV shared activation plus cached BF16 weight in `DSV4Attention.forward` | WQA/WKV/compress `0.123405s` in 07.70 profile | vLLM: quantized `MergedColumnParallelLinear`; mini: FP8 activation fake-quant then cached BF16 `F.linear` | Requires FP8 weight/scale layout and graph-safe activation-scale handling; no cache E2E | Compare `qr/kv` outputs and downstream text smoke | Below single-owner gate, but good first representative for the selected cluster | Yes, but only as part of the cluster target, not standalone. |
| `fused_inv_rope_fp8_quant` | `vllm/v1/attention/ops/deepseek_v4_ops/fused_inv_rope_fp8_quant.py:fused_inv_rope_fp8_quant`; called from `vllm/model_executor/layers/deepseek_v4_attention.py` before `wo_a` | Mini `attn.o_rope`, `wo_a` cached BF16 BMM, then `wo_b` | Current projection surface: `wo_a` `0.064244s`, `wo_b` local `0.051837s`; `o_rope` is outside projection/GEMM | vLLM quantizes attention output to FP8 with per-block scales after inverse RoPE; mini keeps BF16 output projection path | Needs `o_fp8/o_scale` layout and scale handling for `wo_a` einsum; no KV-cache E2E | Compare post-`wo_b` output vs promoted BF16 path, then TP8 text smoke | Under current gate after 07.62; likely less than selected cluster | Yes technically, but not selected because owner is too small now. |
| `deepseek_v4_fp8_einsum` / `wo_a` boundary | `vllm/model_executor/layers/deepseek_v4_attention.py:deepseek_v4_fp8_einsum`; custom op registered in same file | Mini `_wo_a_bf16_bmm_projection` and `dsv4_kernel.wo_a_grouped_projection_fallback` | `wo_a` `0.064244s` current projection/GEMM | FP8 activation plus FP8 `wo_a` weights/scales -> BF16 output before `wo_b`; SM80 source has a dequant/einsum fallback | Needs grouped FP8 scale layout and graph-safe output buffer | Output error after `wo_a` and after `wo_b`; text smoke | Too small as standalone after the 07.62 BF16 BMM promotion | Hold. Not next. |
| Fused indexer q/rope/quant | `vllm/v1/attention/ops/deepseek_v4_ops/fused_indexer_q.py:fused_indexer_q_rope_quant`; called by `DeepseekV4Indexer.forward` | Mini `DSV4Indexer.prepare_fp8_query` and `dsv4_kernel.indexer_q_rope_fp8_fallback` | Current projection pieces: `indexer wq_b` `0.039692s` plus indexer weight/compressor projection `0.044530s`; prior FP8 indexer work already in victory bundle | vLLM folds FP8 Q scale into indexer weights; SM80 path emits fp32 then casts to `float8_e4m3fn` | Requires packed FP8 indexer cache layout and folded weights; mini already has paged FP8 cache pieces | Sparse-index top-k/index overlap, output smoke, text smoke | Below next-target gate unless fresh profile shows indexer/cache has re-grown | Hold. Not next. |
| Packed FP8 indexer/cache continuation | `vllm/model_executor/layers/deepseek_v4_attention.py:DeepseekV4IndexerCache`; `SparseAttnIndexer`; mini source parity from TARGET 07.52 | Mini `python/minisgl/kvcache/deepseek_v4_pool.py` paged FP8 indexer cache and `DSV4AttentionBackend.select_indexer_fp8` | Existing victory-bundle FP8 indexer/cache surface is no longer a large current owner | FP8 or MXFP4 indexer cache with per-row/packed scales | Requires indexer cache allocation/layout; already partly present in mini | Top-k overlap and text smoke | Insufficient current owner size | Do not continue now. |
| Full `fp8_ds_mla` attention KV-cache | `vllm/model_executor/layers/deepseek_v4_attention.py` canonicalizes cache dtype to `fp8_ds_mla`; `vllm/v1/attention/backends/mla/flashmla_sparse.py` | Mini DSV4 attention backend and KV/cache store/select | Not mapped to a concrete `>=0.35s` owner in this pivot without full E2E | FP8 DeepSeek MLA KV-cache contract | Requires full attention cache dtype/layout and backend rewrite | Full attention quality and macro gates | Potentially large, but cannot be isolated cheaply | No. Hard stop for this target. |
| HC `mhc_pre/post` | `vllm/model_executor/layers/mhc.py:mhc_pre`; `torch.ops.vllm.mhc_pre/post` | Mini `DeepseekV4DecoderLayer._hc_pre/_hc_post`; `dsv4_kernel.hc_pre_fallback` | HC pre linear `0.178373s` in 07.69, `0.181483s` in 07.70 candidate profile | vLLM SM80 contract keeps `fn`, `post`, and `comb` FP32-like; mini exact route is aligned except prior cleanup stayed opt-in | No new cache layout | Output comparison and text smoke | TF32 probe gives no decode-small gain; BF16-like changes HC output | No. Stop HC/router lane. |
| MoE `GateLinear` router | `vllm/model_executor/models/deepseek_v4.py:DeepseekV4MoE`; `vllm/model_executor/layers/fused_moe/router/gate_linear.py:GateLinear` | Mini `DSV4MoEGate.forward` and `dsv4_kernel.moe_gate_fallback` | Router `0.097109s` in 07.69, `0.097700s` in 07.70 candidate profile | vLLM asks for router logits dtype `torch.float32`; mini also computes FP32 router logits | No cache layout | Router top-k overlap and text smoke | TF32 has no decode gain; BF16-like changes top-k on larger sample | No. Stop router precision lane. |
| Compile/custom-op boundary | `vllm/model_executor/models/deepseek_v4.py:@support_torch_compile`; `torch.ops.vllm.deepseek_v4_attention` | Mini CUDA graph replay plus Python/source NVTX owner boundaries | No concrete current owner from 07.71 | Mostly boundary/graph visibility, often coupled to FP8 custom ops | Would require broad compile/runtime ownership if not tied to selected cluster | Graph replay and macro gates | Not isolated as a small next target | No. Keep as context only. |

Readout: the only candidate that both maps to a large enough mini surface and
does not require full KV-cache E2E or whole-model compile is the vLLM-aligned
FP8/custom projection-cache cluster.
